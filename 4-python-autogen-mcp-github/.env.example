# Example environment configuration for GitHub Models
# Copy to .env (root) or into server/.env and tools/.env as needed.

#New Relic configuration
# for main app agents.py
#NEW_RELIC_APP_NAME=autogen-agent-app
NEW_RELIC_ENABLED=1
#for tools server
#NEW_RELIC_APP_NAME=agent-mcp-tools
NEW_RELIC_LICENSE_KEY=yourNewRelicIngestLicenseKey
NEW_RELIC_MACHINE_LEARNING_ENABLED=true
NEW_RELIC_AI_MONITORING_ENABLED=true
NEW_RELIC_APPLICATION_LOGGING_ENABLED=true
NEW_RELIC_APPLICATION_LOGGING_FORWARDING_ENABLED=true
NEW_RELIC_APPLICATION_LOGGING_LOCAL_DECORATING_ENABLED=true
NEW_RELIC_APPLICATION_LOGGING_FORWARDING_CONTEXT_DATA_ENABLED=true
NEW_RELIC_APPLICATION_LOGGING_FORWARDING_CONTEXT_DATA_EXCLUDE=*
NEW_RELIC_APPLICATION_LOGGING_FORWARDING_CONTEXT_DATA_INCLUDE=request_id tools model base_url elapsed_ms user_message request_message result result_type message_count response duration_ms port tool_url attempt max_retries

# GitHub Models (OpenAI-compatible) endpoint + auth
OPENAI_API_KEY=ghp_yourTokenWithModelsReadScope
OPENAI_BASE_URL=https://models.inference.ai.azure.com

# Multiple models for performance comparison (comma-separated)
# Each request will use a different model from this list in rotation
# NOTE: Only use models that support function calling (tools)
# NOTE: Only use models available in GitHub Models API
# Valid models: gpt-4o-mini, gpt-4o
LLM_MODELS=gpt-4o-mini,gpt-4o
# Fallback if LLM_MODELS is not set
LLM_MODEL=gpt-4o-mini

# Disable SSL verification (for development/corporate proxy environments only)
# Set to "false" to disable SSL verification
HTTPX_VERIFY=true

# Weather API configuration (optional - for get_weather tool)
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Weather cache TTL in minutes (default: 10)
# How long to cache weather data before fetching fresh data from API
WEATHER_CACHE_TTL_MINUTES=10