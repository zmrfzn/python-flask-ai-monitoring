# Example environment configuration for GitHub Models
# Copy to .env (root) or into server/.env and tools/.env as needed.

#New Relic configuration
# for main app agents.py
NEW_RELIC_APP_NAME=autogen-agent-app

#for tools server
#NEW_RELIC_APP_NAME=agent-mcp-tools
NEW_RELIC_LICENSE_KEY=yourNewRelicIngestLicenseKey

# GitHub Models (OpenAI-compatible) endpoint + auth
OPENAI_API_KEY=ghp_yourTokenWithModelsReadScope
OPENAI_BASE_URL=https://models.inference.ai.azure.com
LLM_MODEL=gpt-4o-mini